import { AppDataSource } from "../data-source";

export const useRepo = () => ({
  // users: AppDataSource.getRepository(),
  // address: AppDataSource.getRepository(),
  // partnerships: AppDataSource.getRepository(),
  // rewards: AppDataSource.getRepository(),
  // matches: AppDataSource.getRepository(),
});
